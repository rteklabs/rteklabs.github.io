// Browser Maps API keys are visible to visitors. Restrict this key in Google Cloud
// to https://rteklabs.github.io/* and to Maps JavaScript API + Places API (New).
// Production deploy replaces this placeholder from the GitHub Actions secret.
// Do not commit a real key here; for local testing edit only your uncommitted copy.
window.PROPERTY_MAP_CONFIG = {
  apiKey: 'YOUR_GOOGLE_MAPS_API_KEY'
};
